{
  "name": "pinch-frame",
  "version": "0.0.0",
  "description": "",
  "main": "pinch-frame.js",
  "types": "pinch-frame.d.ts",
  "prettier": {
    "printWidth": 140,
    "endOfLine": "lf",
    "singleQuote": true,
    "trailingComma": "all"
  },
  "scripts": {
    "start": "esbuild --serve=0.0.0.0:8000 --servedir=. pinch-frame.ts",
    "build": "tsc -p . && esbuild --minify --format=esm --outdir=. pinch-frame.ts"
  },
  "devDependencies": {
    "esbuild": "^0.14.32",
    "prettier": "^2.6.2",
    "typescript": "^4.6.3"
  }
}
