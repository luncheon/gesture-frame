{
  "name": "gesture-frame",
  "version": "0.0.0",
  "description": "",
  "repository": "luncheon/gesture-frame",
  "type": "module",
  "main": "gesture-frame.js",
  "types": "gesture-frame.d.ts",
  "files": [
    "gesture-frame.css",
    "gesture-frame.js",
    "gesture-frame.d.ts",
    "gesture-frame.ts"
  ],
  "prettier": {
    "printWidth": 140,
    "endOfLine": "lf",
    "singleQuote": true,
    "trailingComma": "all"
  },
  "scripts": {
    "start": "esbuild --minify --format=esm --sourcemap --serve=0.0.0.0:8000 --servedir=. gesture-frame.ts",
    "build": "tsc -p . && esbuild --minify --format=esm --outdir=. gesture-frame.ts"
  },
  "devDependencies": {
    "esbuild": "^0.14.36",
    "prettier": "^2.6.2",
    "typescript": "^4.6.3"
  }
}
